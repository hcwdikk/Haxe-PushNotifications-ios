<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<ndll name="pushnotifications" />
    <classpath name ="src"/>
	<!-- Use the following for an Android Java extension, not needed otherwise -->
	
	<dependency name="pushnotifications" path="dependencies/android" if="android" />
	<android extension="org.haxe.extension.PushNotifications" />
	
</project>